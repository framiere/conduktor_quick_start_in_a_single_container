# HA Test: ServiceAccount
apiVersion: messaging.example.com/v1
kind: ServiceAccount
metadata:
  name: ha-test-sa
spec:
  name: ha-test-sa
  dn:
    - "CN=ha-test-sa"
  clusterRef: ha-test-vc
  applicationServiceRef: ha-test-app
