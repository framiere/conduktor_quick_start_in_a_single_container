# Invalid: Tenant B trying to reference Tenant A's ApplicationService
# This should be rejected by the webhook
apiVersion: messaging.example.com/v1
kind: VirtualCluster
metadata:
  name: cross-tenant-vc
spec:
  clusterId: cross-tenant-vc
  applicationServiceRef: tenant-a-app
