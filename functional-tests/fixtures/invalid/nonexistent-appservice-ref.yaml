# Invalid: KafkaCluster referencing non-existent ApplicationService
apiVersion: messaging.example.com/v1
kind: KafkaCluster
metadata:
  name: invalid-vc
spec:
  clusterId: invalid-vc
  applicationServiceRef: non-existent-app
